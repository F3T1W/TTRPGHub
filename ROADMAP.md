# ROADMAP — Таверна Аферистов

> Этот файл — рабочий план «что дальше», в отличие от `PROJECT.md` (видение проекта,
> зафиксированное на старте). Большая часть `PROJECT.md` уже реализована и местами устарела
> (например, там фигурирует MudBlazor — по факту используется Bootstrap 5). `ROADMAP.md`
> отражает реальное текущее состояние и движется вместе с кодом.

---

## Северная звезда

Сделать так, чтобы русскоязычному игроку/мастеру НРИ не нужно было уходить на сторонние
сайты ни для чего: вкат в хобби, изучение правил, билдостроение, поиск пати, ведение игр.
Один сайт закрывает весь цикл от «первый раз слышу про D&D» до «веду кампанию третий год».

---

## Текущее состояние (на момент написания)

Уже реализовано и работает:

| Модуль | Статус |
|---|---|
| Auth (JWT, email-подтверждение, восстановление пароля) | ✅ |
| Персонажи — лист, аватар, PDF/JSON экспорт-импорт | ✅ (ручной ввод, без автоматики) |
| Сессии и кампании (ваншоты + длинные кампании) | ✅ |
| Поиск пати (LFG) с фильтрами | ✅ |
| Виртуальный стол: кубики, чат, шёпот ГМа, синхронная музыка, токены на карте | ✅ |
| Трекер инициативы, энкаунтеры | ✅ |
| Справочник D&D 5e и Pathfinder 2e: заклинания + монстры | ✅ (только это) |
| Форум, Homebrew-хаб, обсуждения, рейтинги, события | ✅ |
| Календарь/напоминания (iCal, Web Push) | ✅ |
| Словарь терминов | ✅ |

**Главные дыры относительно северной звезды:**
1. Справочник правил — только спеллы/монстры. Нет классов, рас, фитов, состояний, снаряжения.
2. Персонаж — плоская анкета, не билдер. Нет автоматики (бонусы расы, прогрессия класса).
3. Всё захардкожено под D&D5e/PF2e отдельными сущностями (`Dnd5eSpell`, `Pf2eSpell`, ...) —
   нет общей системно-независимой модели и нет пути для игры по кастомным/самописным системам.
4. Онбординг для новичков — только глоссарий.
5. Нет системы тикетов/багрепортов от пользователей.
6. Нет ролей (admin/moderator) — некому модерировать контент и тикеты.

---

## Архитектурный принцип: системная независимость

Это ключевое решение, определяющее всю дальнейшую работу с правилами и билдером.

**Сейчас:** `Dnd5eSpell`, `Dnd5eMonster`, `Pf2eSpell`, `Pf2eMonster` — отдельные сущности
с дублирующейся структурой на каждую систему. Добавление новой официальной системы или
кастомной самописной требует копипасты целого вертикального среза (Domain → Persistence → API → Web).

**Куда идём:** единая системно-независимая модель справочника:

```
GameSystem (справочник систем: "dnd5e", "pf2e", "custom:<slug>")
├── Id, Slug, Name, IsOfficial (bool), CreatedByUserId (null для официальных)

RuleEntry (заменяет Dnd5eSpell/Monster и т.д. — одна сущность на любой тип контента)
├── Id, SystemId (→ GameSystem)
├── Category: enum { Spell, Monster, Class, Race, Feat, Condition, Equipment, Background, Rule }
├── Slug, Title, Summary, ContentMarkdown
├── Stats: JSONB (гибкая схема под категорию — для Spell одни поля, для Monster другие)
├── Tags: string[]
└── IsHomebrew: bool (отличает официальный контент от пользовательского)
```

Почему JSONB для `Stats`, а не строгая EF-схема под каждую категорию: количество комбинаций
(категория × система) растёт неограниченно при добавлении кастомных систем, а строгая
реляционная схема под каждую — это та же проблема дублирования, только глубже. Гибкие поля
(`Stats` как JSONB + типизированный C#-DTO поверх для известных официальных категорий)
дают системную независимость без потери строгой типизации там, где она нужна (D&D5e/PF2e).

**Переходный путь (не революция, а эволюция):**
1. Новый `RuleEntry` + `GameSystem` живут рядом со старыми `Dnd5eSpell`/`Pf2eSpell` — ничего не ломаем
2. Пишем миграционный скрипт, переносящий существующие D&D5e/PF2e данные в `RuleEntry`
3. Когда фронтенд и API переключены на `RuleEntry`, старые таблицы выпиливаем
4. Homebrew-хаб (он уже умеет произвольный контент) становится фронтендом для создания
   кастомных `GameSystem` + `RuleEntry` — переиспользуем существующую инфраструктуру вместо
   написания новой с нуля

---

## Фаза A — Справочник правил 2.0

Цель: закрыть главный геп — «изучение правил». Приоритет №1, потому что без справочника
невозможен билдер (фаза B) — нечем будет питать автоматику.

- [x] `GameSystem` + `RuleEntry` сущности, миграция (Domain/Persistence) — `AddRuleEntrySystem`
- [x] Перенос D&D5e/PF2e спеллов и монстров на новую модель — `LegacyRuleMigrator`,
      legacy-таблицы и старые эндпоинты оставлены как есть (аддитивно)
- [x] Категории контента, добавленные напрямую в `RuleEntry`: классы, расы, фиты, состояния
      (снаряжение и предыстории — ещё не сделаны, Open5e classes/equipment требует отдельного разбора)
- [x] Импорт классов/рас/фитов/состояний D&D5e через Open5e API — `Open5eRulesImporter`
- [x] Классы/расы для PF2e — `Pf2eRulesSeeder`: 6 классов (Боец/Волшебник/Плут/Жрец/
      Следопыт/Варвар) и 6 предков (Человек/Эльф/Дварф/Полурослик/Гном/Полуорк),
      написаны вручную сразу на русском (не через машинный перевод — механика PF2e
      сильно отличается от D&D5e, готовых английских данных с той же структурой нет).
      Пока справочник-only, не подключены к мастеру создания персонажа (см. фазу B)
- [x] UI: единый справочник `/reference/{system}/rules/{category}` (список + деталь) — работает
      для любой категории/системы без дублирования кода. Существующие `/spells`/`/monsters`
      страницы не тронуты, только добавлена быстрая навигация между ними
- [x] Рендер `StatsJson` через Markdig — markdown-таблицы прогрессии классов рендерятся как
      настоящие HTML-таблицы в теме сайта, длинные/табличные stat-поля выделены отдельными блоками
- [x] **Локализация справочника D&D5e**: весь контент из Open5e (англоязычный источник) —
      названия, описания, характеристики монстров (включая вложенные действия/способности),
      классы/расы/фиты/состояния — переводится на русский при импорте (`ITranslationService` →
      бесплатный Google Translate endpoint, без ключа). PF2e не требовал перевода — контент
      написан вручную сразу на русском. Legacy-таблицы (`dnd5e_spells`/`dnd5e_monsters`) и
      `RuleEntry` содержат уже переведённые данные — перевод происходит один раз при импорте,
      не на горячем пути запросов. Slug остаётся на латинице от оригинального английского имени
      (стабильные URL), отображаемые названия — на русском
- [x] Кастомные системы: `POST /api/v1/rules/systems` создаёт `GameSystem.CreateCustom` (слаг —
      транслитерация названия + де-дубликация суффиксом `-2`, `-3`...), `POST/PUT/DELETE
      /api/v1/rules/{systemSlug}/{category}` управляют `RuleEntry` внутри своей системы.
      Построено параллельно старому Homebrew-хабу (форумный "один блоб текста"), не поверх
      него — Homebrew не имеет структурированной `Category`/`StatsJson`/Markdig-рендера и не
      может отдавать данные в унифицированный `/reference/{system}/rules/{category}` без
      переписывания. Авторизация: владеет записями тот, кто `GameSystem.CreatedByUserId`
      (per-entry владения нет, `RuleEntry` не хранит своего автора — осознанное упрощение);
      `Error.Forbidden()` → HTTP 403 (заодно исправлена недостающая мапа в `ToProblem()` —
      раньше 403 не существовал, всё falls back на 400). Официальные системы (`IsOfficial=true`)
      неприкосновенны — попытка добавить/изменить/удалить запись в dnd5e/pf2e даёт 403.
      UI: `/reference/create-system` (форма создания), кнопка «Своя система» на `/reference`,
      кастомные системы рядом с официальными карточками с бейджем «Homebrew · моя»/«Homebrew»;
      `/reference/{system}/rules/{category}/create` и `.../{slug}/edit` — общая форма (категория,
      название, краткое и полное описание в Markdown, `StatsJson` как сырой JSON-textarea,
      теги через запятую); кнопки редактировать/удалить на `Detail.razor` видны только владельцу
      (`RuleEntryDetailDto.CanEdit`). Для кастомных систем список категорий на `List.razor`
      расширен до всех 9 (включая Spell/Monster/Equipment/Background) — у официальных систем
      навигация не меняется (там Spell/Monster живут на отдельных legacy-страницах `/spells`,
      `/monsters`). Проверено end-to-end curl + браузером: создание системы → транслитерация
      слага → добавление монстра → просмотр → 403 на чужую/официальную систему → правка
      владельцем → удаление владельцем → 404 после удаления

## Фаза B — Билдостроение

Зависит от фазы A (нужны классы/расы/фиты как структурированные данные, не текст).

- [x] Мастер создания персонажа (`/characters/create-wizard`): выбор расы/класса из `RuleEntry`
      (пока только D&D5e — там есть данные классов/рас) → авто-применение расовых бонусов
      характеристик, HP по кости хитов класса + мод. Телосложения, AC = 10 + мод. Ловкости,
      спасброски класса. `CharacterAutomationCalculator` — чистая функция, легко тестируется
- [x] Обратная совместимость сохранена без изменений в домене: мастер лишь оркестрирует уже
      существующие `Character.Create` + `Character.UpdateSheet`, старая ручная анкета
      (`/characters/create`) не тронута и работает как раньше, обе ссылаются друг на друга
- [x] Прогрессия по уровням: `POST /api/characters/{id}/level-up` пересчитывает MaxHitPoints
      по кости хитов класса (ищет класс персонажа в `RuleEntry` по точному совпадению названия)
      и best-effort достаёт «что нового» из markdown-таблицы прогрессии класса
      (`CharacterAutomationCalculator.FindLevelFeatures`). UI: кнопка «Уровень» на странице
      персонажа. Полный ручной разбор фитов/слотов заклинаний за уровень — не сделан,
      это следующий шаг данной фичи, если понадобится глубже
- [x] Мультикласс-калькулятор — `/tools/multiclass`, standalone (не трогает `Character`,
      чтобы не рисковать доменом): считает суммарный уровень, бонус мастерства и пул костей
      хитов по правилам D&D5e мультикласса (первый уровень первого класса — максимум кости,
      остальные — среднее)
- [x] Мастер для PF2e — `CreateCharacterFromRulesCommandHandler` теперь ветвится по `SystemSlug`
      (`"pf2e"` → `Pf2eCharacterAutomationCalculator`, иначе прежний D&D5e-путь). Новый калькулятор
      **сознательно упрощён**, не претендует на полную точность PF2e-правил — задокументировано
      прямо в коде и в подсказке мастера персонажа:
      - Буст характеристик: применяются только явные пары предок/класс (`boost_codes`/`flaw_code`
        предка + `key_ability_codes` класса, если код один) по стандартной формуле PF2e
        (+2 если текущее значение <18, иначе +1; провал предка −2). Свободный выбор буста
        (`"ANY"` у Человека/Полуорка, второй ключевой атрибут у Бойца/Следопыта) **не
        применяется автоматически** — игрок поднимает нужную характеристику вручную до отправки формы
      - HP = HP предка + уровень × (HP класса за уровень + мод. Телосложения) — верная формула PF2e
      - AC = 10 + мод. Ловкости + (уровень + 2) — предполагает "тренированное" владение обороной
        без доспеха/щита; ranks владения (untrained/trained/expert/master/legendary) по отдельным
        показателям не моделируются, потому что `Character`-сущность хранит один плоский
        `ProficiencyBonus` в D&D5e-стиле (уровень→бонус по таблице 5e) — полная переработка модели
        персонажа под PF2e ranks не входила в скоуп этой итерации
      - `Character.SavingThrowProficiencies` остаётся пустым списком для PF2e-персонажей
        (вместо того чтобы подделывать несуществующее бинарное владение спасброском)
      - `Pf2eRulesSeeder` дополнен структурированными кодами (`key_ability_codes`,
        `boost_codes`, `flaw_code` — рядом с прежним читаемым текстом `key_ability`/`boosts`/`flaw`
        для отображения в справочнике)
      - UI: `/characters/create-wizard` — добавлен селектор системы (D&D 5e / Pathfinder 2e),
        переключение перезагружает списки рас/классов из справочника; при выборе PF2e показывается
        предупреждение об упрощениях
      - Проверено end-to-end curl (Дварф-Варвар 1 уровня: буст CON+2/WIS+2, провал CHA−2, буст
        класса STR+1 — все числа сошлись; Человек-Волшебник 5 уровня: свободный буст предка не
        применён, буст класса INT+1 применён, HP=43 и AC=19 математически верны) + браузером
        (переключение системы подгружает списки предков/классов PF2e)
- [x] **Проблема качества данных с терминологией — исправлено**: причина была в
      `JsonTranslationHelper`, который переводил вообще все строковые поля, включая игровую
      нотацию костей (`hit_dice`/`damage_dice`, например "1d12") — Google Translate иногда
      портил латинскую `d` на кириллическую `д`, из-за чего `HitDieRegex` не матчился и HP
      автоматики тихо считался по дефолтной d8. Добавлен параметр `excludeKeys` — эти поля
      больше не переводятся при импорте. Плюс одноразовый SQL-проход нормализовал уже
      занесённые данные: капитализация первой буквы у переведённых названий и унификация
      "Конституция"/"Телосложение" → "Телосложение" везде. Полный переимпорт D&D5e данных
      прогнан и проверен (класс Варвар: `hit_dice` = "1d12", левел-ап 4→8 даёт корректные 85 HP)

## Фаза C — Онбординг

Низкая техническая сложность, высокая ценность для удержания новичков.

- [x] Раздел `/start` — «с чего начать»: разные ветки для «я игрок» / «я мастер» (переключатель
      ролей, шаги со ссылками на реальные разделы сайта — справочник, мастер персонажа, ЛФГ и т.д.)
- [x] Гайд по сессии-ноль, базовым механикам и памятка новому мастеру — реализовано именно так,
      как предполагалось ниже: три лонгрида как `RuleEntry` (Category=Rule) в новой системной
      записи `guides` (`GameSystem.CreateOfficial("guides", "Гайды и памятки")` — не игровая
      система, а просто контейнер для статей), засеяны через новый `GuidesSeeder`
      (`src/Infrastructure/TTRPGHub.Persistence/Seeding/GuidesSeeder.cs`), идемпотентный как
      остальные сидеры. Ничего нового в инфраструктуре не потребовалось — статьи рендерятся
      через уже существующий `/reference/{system}/rules/{category}/{slug}` с Markdig (чек-листы,
      таблицы, внутренние ссылки на `/sessions`, `/glossary` и т.д. — всё уже работало):
      - `session-zero` — что обсудить перед стартом кампании (границы, тон, ожидания, хоумрулы)
      - `gm-first-session` — чек-лист подготовки + что делать, если игроки пошли не туда
      - `player-basics` — броски d20, модификаторы характеристик (с таблицей), инициатива, HP
      Ссылки на все три добавлены в существующие шаги `/start` (замена generic-текста на конкретные
      гайды у шагов «Сессия-ноль» и «Чек-лист перед сессией» у мастера, новый шаг «Базовые механики»
      у игрока), сам `/start` остался статичным Razor-компонентом — это осознанно, дальнейшая
      миграция шагов `/start` в `RuleEntry` не требуется, там уже просто ссылки на реальные разделы
- [x] Реализовано как специальная категория `RuleEntry` (Category=Rule, не привязана к конкретной
      игровой системе) — переиспользована инфраструктура фазы A вместо отдельного движка статей,
      как и предполагалось изначально
- [ ] Список гайдов (`/reference/guides/rules/rule`) не виден через вкладки навигации на самой
      странице списка — `List.razor` показывает вкладки категорий (класс/раса/фит/состояние) только
      для официальных D&D5e/PF2e-подобных систем; «guides» физически доступна по прямой ссылке
      и так на неё и ведут все переходы, но самостоятельно найти её через справочник (не с `/start`)
      пока нельзя — мелкая навигационная дыра, не блокирует использование

## Фаза D — Система тикетов / багрепортов

Канбан-доска для пользовательских багрепортов: **Открыт → В работе → Готово**.

### Domain
- [x] `SupportTicket` агрегат: `Id`, `ReporterId (UserId)`, `Title`, `Description`,
      `Status (enum: Open, InProgress, Done)`, `ContactInfo (string?)`, `CreatedAt`, `UpdatedAt`
- [x] `TicketAttachment`: `Id`, `Url`, `FileName`, `ContentType` — owned-коллекция на `SupportTicket`
      (1 тикет — много вложений, отдельная таблица `ticket_attachments`)
- [ ] `TicketComment` (опционально, фаза D.2): переписка по тикету между автором и модератором —
      не сделано, вне минимального скоупа канбана

### Предпосылка: роли пользователей
- [x] `User.Role: enum { Player, Moderator, Admin }` (default `Player`) — миграция
      `AddSupportTicketsAndUserRole`. Роль прокидывается в JWT (`ClaimTypes.Role`), стандартный
      `RequireRole("Moderator", "Admin")` защищает эндпоинты — назначение роли пока только вручную в БД,
      отдельного UI/эндпоинта повышения до модератора нет (сознательно — не было запрошено)
- [x] Авторизационная политика: `.RequireAuthorization(p => p.RequireRole("Moderator", "Admin"))`
      на эндпоинтах списка всех тикетов и смены статуса — проверено curl (Player получает 403)

### API
- [x] `POST /api/tickets` — создать (title, description, contactInfo, multipart-вложения через
      `IFormFileCollection`), бакет MinIO `tickets`, лимит 10 МБ/файл и 5 файлов на тикет
- [x] `GET /api/tickets/me` — мои тикеты, с пагинацией
- [x] `GET /api/tickets` — все тикеты (только Moderator/Admin) — данные для канбан-доски
      (без пагинации — доска показывает всё сразу по колонкам, объём для внутреннего инструмента невелик)
- [x] `PATCH /api/tickets/{id}/status` — сменить колонку (только Moderator/Admin)
- [ ] `POST /api/tickets/{id}/comments` — комментарий к тикету — не сделано вместе с `TicketComment` (D.2)

### UI
- [x] `/tickets` — личный кабинет: форма создания (заголовок, описание, контакт, до 5 вложений
      через `InputFile`) + список своих тикетов со статусом и ссылками на вложения
- [x] `/admin/tickets` — канбан-доска (3 колонки), видна только `Moderator`/`Admin`
      (`[Authorize(Roles="Moderator,Admin")]` + `<AuthorizeView>` в `NavMenu` скрывает пункт меню
      от остальных). Drag-and-drop — нативные HTML5 `draggable`/`ondragstart`/`ondragover`/`ondrop`
      в разметке Blazor, без кастомного JS-интеропа (в отличие от токенов на столе — там нужна
      свободная позиция x/y и точность, тут только перенос между 3 колонками, DOM-события хватает)
- [x] Проверено end-to-end: создание тикета с вложением → файл доступен по прямой ссылке из MinIO →
      модератор видит его на канбан-доске → смена статуса через drag-and-drop обновляет БД →
      пользователь видит новый статус в своём кабинете

## Фаза E — Полировка существующего (низкий приоритет, уже функционально)

Эти направления уже закрыты на базовом уровне; ниже — что можно докрутить, когда основные
гепы (A–D) закрыты:

- [ ] VTT: полноценные листы персонажей прямо за столом (не только токены)
- [ ] VTT: туман войны / ограничение видимости для ГМа
- [ ] Party finding: отзывы/рейтинг конкретно по сыгранным сессиям, а не только общий рейтинг пользователя
- [ ] Мультиязычность (изначально заложена в принципах PROJECT.md, но не приоритет, пока не закрыт RU-рынок)

## Фаза F — Оффлайн-игра

До этого весь функционал (LFG, события) был по факту заточен под онлайн: `GameEvent.Format`
существовал (Online/Offline/Hybrid), но нигде не использовался как фильтр, а `GameSession`
(на чём построен LFG `/party`) вообще не имел понятия локации.

- [x] `GameSession`: добавлены `Format (SessionFormat: Online/Offline/Hybrid)` и `Location (string?)`
      — миграция `AddSessionLocationAndFormat`. Раньше сессия могла быть только "где-то в дискорде"
      по умолчанию без явного проговаривания формата
- [x] `GameEvent`: `Location`/`Format` уже существовали в домене (заложены заранее), но никогда
      не были доступны как параметры фильтрации — теперь `GetEventsQuery`/`IGameEventRepository`
      принимают опциональные `Location`/`Format`, `GET /api/v1/events?location=...&format=...`
- [x] `GET /api/sessions/upcoming?location=...&format=...` — фильтрация на стороне БД
      (`EF.Functions.ILike` по подстроке для локации, точное совпадение для формата)
- [x] UI `/events`: добавлен блок фильтров (город/место + формат) — раньше страница вообще
      не имела фильтров, только пагинация
- [x] UI `/party` (LFG): добавлены фильтры «Формат» и «Город» рядом с уже существующими
      (система, свободные места) — City/Location теперь показываются на карточке сессии
- [x] Форма создания сессии (`/sessions/create`) и создания события — поля формата и
      города/места для организатора
- [ ] Геолокация/структурированные города (сейчас — свободный текст с ILIKE-поиском по
      подстроке, без нормализации «Москва» vs «москва» vs «мск», без привязки к `User.Profile.City`
      как источнику для авто-подсказки при создании сессии/события)
- [ ] Карта локаций / поиск «рядом со мной» — не сделано, нужна геокодировка городов в
      координаты, что выходит за рамки простого текстового фильтра
- [ ] Оффлайн-специфичные форматы контента: распечатываемые карточки персонажа, трекер
      инициативы на телефоне ГМа без полноценного VTT — пока предполагается, что оффлайн-группы
      просто используют существующий билдер персонажа и справочник, отдельного оффлайн-режима нет

## Фаза G — Модерация контента и управление ролями

До этого `User.Role` (Player/Moderator/Admin) существовал только для тикетов — назначить роль
можно было исключительно вручную через SQL, а форум/homebrew/отзывы/обсуждения вообще не имели
модераторского обхода при удалении (только сам автор мог удалить своё, форум — вообще без удаления).

- [x] Управление ролями: `GET /api/v1/users/admin?search=&page=&pageSize=` (список пользователей,
      только Admin) + `PATCH /api/v1/users/admin/{id}/role` (сменить роль, только Admin).
      UI: `/admin/users` — таблица с инлайн-селектом роли. Защита от самоблокировки: нельзя
      изменить собственную роль (иначе последний админ мог случайно понизить сам себя без возможности
      восстановления кроме SQL)
- [x] Moderator-обход при удалении контента — унифицирован по всем существующим `Delete*CommandHandler`:
      `if (author != currentUser.Id && !isModerator) return Error.Forbidden();` — применено к
      Homebrew (`DeleteHomebrewCommandHandler`, заодно унифицирован код ошибки — было `Error.Validation`,
      стало `Error.Forbidden()` как везде), Ratings (`DeleteRatingCommandHandler`),
      Discussions (`DeletePostCommandHandler`)
- [x] Форум: удаления не было вообще ни для кого — добавлены `DeleteTopicCommand`/`DeletePostCommandHandler`
      (папка `Features/Forum/Commands/Delete{Topic,Post}`) с тем же авторско-модераторским чеком,
      `DELETE /api/v1/forum/topics/{id}` и `/posts/{id}`. Кэскейд тема→посты уже был настроен в EF
      (`ForumTopicConfiguration`), поэтому удаление темы с постами не потребовало ручной чистки
- [x] Система жалоб — новый агрегат `ContentReport` (`ReporterId`, `EntityType` enum
      {ForumPost, ForumTopic, HomebrewItem, Rating, DiscussionPost}, `EntityId`, `Reason`,
      `Status` {Open, Resolved, Dismissed}) — миграция `AddContentModeration`.
      `POST /api/v1/reports` (любой авторизованный), `GET /api/v1/reports` (только Moderator/Admin,
      только открытые), `PATCH /api/v1/reports/{id}/resolve` (Resolved/Dismissed). UI: кнопка
      «Пожаловаться» на постах форума → `/admin/reports` — очередь модератора с кнопками
      Решено/Отклонить
- [x] Проверено end-to-end curl + браузером: Admin меняет роль на Moderator → модератор видит
      список открытых жалоб → модератор удаляет чужой homebrew-предмет и чужую тему форума (204) →
      игрок не может ни то ни другое (403) → игрок создаёт жалобу → модератор её разрешает →
      жалоба пропадает из очереди
- [ ] Report-кнопка добавлена только на посты форума — Homebrew/Ratings/Discussions можно
      репортить через API (`POST /api/v1/reports` с соответствующим `EntityType`), но кнопки
      в UI для них ещё нет (сознательно сокращённый скоуп — форум был приоритетом, т.к. там
      не было вообще никакой модерации)
- [ ] `ForumTopic.Pin()/Unpin()/Lock()/Unlock()` — методы существуют в домене (были добавлены
      заранее), но команд/эндпоинтов для них так и нет — закрепление и блокировка тем всё ещё
      недоступны модераторам через UI/API
- [ ] Нет журнала модераторских действий (кто что удалил/разрешил) — `ContentReport.ResolvedByUserId`
      фиксируется, но для удалений контента (Homebrew/Forum/Ratings/Discussions) факт "модератор
      X удалил Y" нигде не логируется отдельно от обычного soft/hard delete

---

## Зависимости между фазами

```
Фаза A (Справочник 2.0) ──→ Фаза B (Билдостроение)
Фаза C (Онбординг) ──→ переиспользует RuleEntry из фазы A, но может стартовать параллельно
Фаза D (Тикеты) ──→ независима, требует только добавления ролей пользователей
Фаза E ──→ после A–D, не блокирует и не блокируется
```

Рекомендуемый порядок старта: **A → D (можно параллельно, т.к. независима) → B → C → E**.

---

*Документ живёт вместе с кодом. Обновлять при завершении пунктов и появлении новых гепов.*
*Создан: 2026-07-01*
